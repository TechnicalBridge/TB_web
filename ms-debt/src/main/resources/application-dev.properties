# =============================================================================
#  Perfil dev: para programar. Se activa con SPRING_PROFILES_ACTIVE=dev
#      .\mvnw.cmd -pl ms-debt spring-boot:run "-Dspring-boot.run.profiles=dev"
# =============================================================================

logging.level.com.tbridge=DEBUG
logging.level.org.hibernate.SQL=DEBUG
spring.jpa.properties.hibernate.format_sql=true
management.endpoint.health.show-details=always

#  Los eventos a APOFYX salen cada dos segundos en vez de quince.
app.eventos.intervalo-ms=2000
